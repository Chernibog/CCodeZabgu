/*

Автор: Шумилко Н.Д. (4 вариант) (https://github.com/VetrovSV/OOP/blob/master/C/tasks.md)          <- ссылка на табличку с заданиями
                                  (https://bgu-chita.ru/zadachnik/Glava01/index01.htm#z75)        <- ссылка на задание (найти самому)

Задача 75:
Доказать, что любую целочисленную денежную сумму, большую 7 руб., можно выплатить без сдачи трешками и пятерками. Для данного n > 7 найти такие целые неотрицательные а и b, что Зa + 5b = n.
*/

#include <stdio.h>   // Подключаем стандартную библиотеку для работы с вводом-выводом (printf, scanf и т.д.)
#include <windows.h> // Подключаем библиотеку Windows для настройки кодировки консоли

int main() {
    // Установка кодировки вывода консоли на UTF-8 для корректного отображения русских букв
    SetConsoleOutputCP(CP_UTF8);

    int n;          // Переменная для хранения введённого пользователем числа (>7)
    int a, b;       // Переменные для количества троек и пятёрок соответственно
    int found = 0;  // Флаг, указывающий, было ли найдено решение (1 - да, 0 - нет)

    // Запрашиваем у пользователя ввести число n
    printf("Введите целое число n (>7): ");
    scanf("%d", &n); // Считываем значение из консоли

    // Проверяем условие: число должно быть больше 7
    if (n <= 7) {
        printf("Ошибка: n должно быть больше 7.\n");
        return 1; // Возвращаем код ошибки и завершаем программу
    }

    // Перебираем возможные значения b — количество пятёрок (от 0 до максимально возможного)
    for (b = 0; b <= n / 5; b++) {
        // Вычисляем остаток после вычитания пятёрок (5*b), проверяем делится ли он нацело на 3
        if ((n - 5 * b) % 3 == 0 && (n - 5 * b) >= 0) {
            // Если делится, то это подходящее решение:
            a = (n - 5 * b) / 3; // Находим количество троек
            // Выводим найденное решение
            printf("Решение найдено: 3*%d + 5*%d = %d\n", a, b, n);
            found = 1; // Устанавливаем флаг, что решение найдено
            break;     // Прерываем цикл, так как нам нужно одно любое решение
        }
    }

    // По теореме, при n > 7 решение всегда существует, но на случай ошибки добавляем проверку
    if (!found) {
        printf("Ошибка: Решение не найдено, хотя при n > 7 оно всегда должно существовать.\n");
    }

    return 0; // Завершаем программу с кодом 0 (успешно)
}