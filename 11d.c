/*

Автор: Шумилко Н.Д. (4 вариант) (https://github.com/VetrovSV/OOP/blob/master/C/tasks.md)          <- ссылка на табличку с заданиями
                                  (https://bgu-chita.ru/zadachnik/Glava01/index01.htm#z11)        <- ссылка на задание (найти самому)

Задача 11д:
Даны x, y, z. Вычислить a, b, если a = y + [x / (y² + |(x² / (y + x³ / 3))|)];b = 1 + [(tan(z/2)/2)^2].

*/

#include <stdio.h>   // Библиотека для работы с функциями ввода/вывода (например, printf и scanf)
#include <math.h>    // Библиотека математических функций (pow, abs, tan и т.д.)
#include <windows.h> // Библиотека Windows для настройки кодировки консоли

int main() {
    // Объявление переменных типа float (числа с плавающей точкой)
    float a, b, x, y, z;

    // Установка кодировки вывода в консоль на UTF-8 для корректного отображения кириллицы
    SetConsoleOutputCP(CP_UTF8);

    // Вывод сообщения пользователю о необходимости ввести значения x, y, z
    puts("Введите x,y,z");

    // Считывание трёх чисел с плавающей точкой из стандартного ввода
    // Пользователь должен ввести их через пробел или перевод строки
    scanf("%f %f %f", &x, &y, &z);

    // Вычисление выражения для переменной a:
    // a = y + [x / (y² + |(x² / (y + x³ / 3))|)]
    a = y + (x / (pow(y, 2) + fabs(pow(x, 2) / (y + pow(x, 3) / 3))));

    // Вычисление выражения для переменной b:
    // b = 1 + [(tan(z/2)/2)^2]
    // Используется tan — тангенс, и pow — возведение в степень
    b = 1 + pow(tan(z / 2) / 2, 2);

    // Вывод результата вычисления выражения a
    printf("%f\n", a);

    // Вывод результата вычисления выражения b
    printf("%f\n", b);

    // Завершение программы с кодом 0 (успешное выполнение)
    return 0;
}